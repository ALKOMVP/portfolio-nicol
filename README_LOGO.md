# 📸 Logo

## Ubicación del Logo

El logo debe estar ubicado en:
```
public/logo.png
```

## Instrucciones

1. Coloca el archivo `logo.png` en la carpeta `public/` del proyecto
2. El logo se mostrará automáticamente en el navegador junto al nombre "Nicol Mena"
3. El tamaño recomendado es aproximadamente 48x48 píxeles (o proporcional)
4. El logo debe tener fondo transparente (PNG con transparencia)

## Si el logo no aparece

Si el logo no se muestra:
1. Verifica que el archivo existe en `public/logo.png`
2. Verifica que el nombre del archivo sea exactamente `logo.png` (minúsculas)
3. Recarga la página con Ctrl+Shift+R (o Cmd+Shift+R en Mac) para limpiar el caché



